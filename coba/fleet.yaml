namespace: default
targetCustomizations:
- name: aws
  helm:
    values:
      host: aws.sigmatech.microgen.id
  clusterSelector:
    matchLabels:
      env: aws

- name: microgen
  helm:
    values:
      host: sigmatech.microgen.id
  clusterSelector:
    matchLabels:
      env: microgen
